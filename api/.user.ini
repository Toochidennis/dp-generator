; Template/banner images are sent as base64 data: URLs in the JSON body, so
; both limits need headroom over the 12MB image cap enforced in helpers.php.
; Only takes effect on PHP-FPM hosts that honor .user.ini (most shared hosting).
post_max_size = 20M
upload_max_filesize = 20M
memory_limit = 128M
